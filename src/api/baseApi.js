/**
 * Defining API version to use against randomuser.me
 * @type {string}
 */
const API_VERSION = '1.3';

/**
 * Base URL to the random user server
 * @type {string}
 */
export const BASE_URL = `https://randomuser.me/api/${API_VERSION}`;
